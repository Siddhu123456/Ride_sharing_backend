"""Fleet-owner models package.
This package contains models that were moved from the top-level `app.models` into
a domain-specific package. Keep this file minimal to avoid import side-effects.

Use top-level shims in `app/models/*.py` to preserve existing import paths.
"""

__all__ = []
